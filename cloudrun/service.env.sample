# Non-secret envs (example). Copy & edit; wire them in the deploy step if needed.
NODE_ENV=production
CORS_ORIGIN=*

# Firestore (optional, lazy init). If empty, /health.firestore יחזור no-project
FIREBASE_PROJECT_ID=wizbi-cp

# BigQuery defaults (להמשך)
BQ_DEFAULT_DATASET=ai_unified
BIGQUERY_LOCATION=US
