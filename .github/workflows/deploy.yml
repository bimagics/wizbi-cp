name: Deploy (Cloud Run + Hosting)

on:
  push:
    branches: [ "dev", "main" ]
  workflow_dispatch: {}

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

env:
  AR_REPO: wizbi
  IMAGE_NAME: cp-unified
  HOSTING_SITE_PROD: wizbi-cp
  HOSTING_SITE_QA: wizbi-cp-qa
  ADMINS: "omer.cohen@bimagics.com"

jobs:
  build-and-deploy-cloud-run:
    name: Build & Deploy Cloud Run
    runs-on: ubuntu-latest
    outputs:
      service_url: ${{ steps.service_url.outputs.url }}
    env:
      PROJECT_ID:   ${{ secrets.GCP_PROJECT_ID }}
      REGION:       ${{ secrets.GCP_REGION }}
      WIF_PROVIDER: ${{ secrets.WIF_PROVIDER }}
      DEPLOYER_SA:  ${{ secrets.DEPLOYER_SA }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth to GCP (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.DEPLOYER_SA }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Ensure Artifact Registry repo exists (idempotent)
        run: |
          set -e
          if ! gcloud artifacts repositories describe "${{ env.AR_REPO }}" --location "${{ env.REGION }}" >/dev/null 2>&1; then
            gcloud artifacts repositories create "${{ env.AR_REPO }}" \
              --repository-format=DOCKER \
              --location "${{ env.REGION }}" \
              --description "WizBI images"
          fi

      - name: Configure Docker to Artifact Registry
        run: gcloud auth configure-docker "${{ env.REGION }}-docker.pkg.dev" -q

      - name: Compute image tags
        id: meta
        run: |
          SHA_SHORT="${GITHUB_SHA::7}"
          TS="$(date +%s)"
          BASE="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPO }}/${{ env.IMAGE_NAME }}"
          echo "image_sha=${BASE}:sha-${SHA_SHORT}"             >> $GITHUB_OUTPUT
          echo "image_branch=${BASE}:branch-${GITHUB_REF_NAME}" >> $GITHUB_OUTPUT
          echo "image_ts=${BASE}:${TS}"                          >> $GITHUB_OUTPUT
          echo "image_base=${BASE}"                              >> $GITHUB_OUTPUT

      - name: Build and Push Image
        run: |
          docker build -t "${{ steps.meta.outputs.image_branch }}" .
          docker push "${{ steps.meta.outputs.image_branch }}"

      - name: Decide target service (QA vs PROD)
        id: svc
        run: |
          if [[ "${GITHUB_REF_NAME}" == "dev" ]]; then
            echo "service=cp-unified-qa"  >> $GITHUB_OUTPUT
          else
            echo "service=cp-unified"     >> $GITHUB_OUTPUT
          fi

      - name: Deploy to Cloud Run (pinned by digest)
        run: |
          gcloud run deploy "${{ steps.svc.outputs.service }}" \
            --image "${{ steps.meta.outputs.image_branch }}" \
            --region "${{ env.REGION }}" \
            --service-account "wizbi-runner@${{ env.PROJECT_ID }}.iam.gserviceaccount.com" \
            --no-allow-unauthenticated \
            --update-env-vars "NODE_ENV=production,FIREBASE_PROJECT_ID=${{ env.PROJECT_ID }},CORS_ORIGIN=*,WHATSAPP_ENABLED=false,ADMINS=${{ env.ADMINS }}"

      - name: Get service URL
        id: service_url
        run: |
          URL=$(gcloud run services describe "${{ steps.svc.outputs.service }}" --region "${{ env.REGION }}" --format='value(status.url)')
          echo "Deployed: $URL"
          echo "url=$URL" >> $GITHUB_OUTPUT

  deploy-hosting-and-test:
    name: Deploy Hosting + Authenticated Test
    needs: build-and-deploy-cloud-run
    runs-on: ubuntu-latest
    env:
      PROJECT_ID:     ${{ secrets.GCP_PROJECT_ID }}
      REGION:         ${{ secrets.GCP_REGION }}
      WIF_PROVIDER:   ${{ secrets.WIF_PROVIDER }}
      DEPLOYER_SA:    ${{ secrets.DEPLOYER_SA }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Auth to GCP (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.DEPLOYER_SA }}
          project_id: ${{ env.PROJECT_ID }}
      
      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Install Firebase CLI
        run: npm i -g firebase-tools@latest

      - name: Decide Hosting site (QA/PROD)
        id: host
        run: |
          if [[ "${GITHUB_REF_NAME}" == "dev" ]]; then
            echo "site=${{ env.HOSTING_SITE_QA }}"  >> $GITHUB_OUTPUT
            echo "url=https://${{ env.HOSTING_SITE_QA }}.web.app" >> $GITHUB_OUTPUT
          else
            echo "site=${{ env.HOSTING_SITE_PROD }}" >> $GITHUB_OUTPUT
            echo "url=https://${{ env.HOSTING_SITE_PROD }}.web.app" >> $GITHUB_OUTPUT
          fi

      - name: Deploy Hosting (uses WIF credentials)
        run: |
          firebase deploy \
            --only "hosting:${{ steps.host.outputs.site }}" \
            --project "${{ env.PROJECT_ID }}" \
            --non-interactive

      - name: Authenticated smoke test via Cloud Run URL
        run: |
          set -e
          SERVICE_URL="${{ needs.build-and-deploy-cloud-run.outputs.service_url }}"
          echo "Testing service at: $SERVICE_URL"
          TOKEN=$(gcloud auth print-identity-token --audiences="$SERVICE_URL" --impersonate-service-account="${{ env.DEPLOYER_SA }}")
          CODE=$(curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $TOKEN" "$SERVICE_URL/health")
          echo "Authenticated health check => $CODE"
          if [ "$CODE" != "200" ]; then
            echo "::error::Authenticated health check failed with code $CODE"
            exit 1
          fi

      - name: Print Hosting URL
        run: |
          echo "Hosting URL: ${{ steps.host.outputs.url }}/admin"
